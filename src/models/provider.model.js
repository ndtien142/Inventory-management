"use strict";

class Provider {
    constructor(providerId, providerName, providerAddress) {
        this.providerId = providerId;
        this.providerName = providerName;
        this.providerAddress = providerAddress;
    }
}

module.exports = Provider;
